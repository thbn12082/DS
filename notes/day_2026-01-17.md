FT-W1D1 — 2026-01-17 — NumPy khởi động (array, shape, slicing, vectorized ops)

Done: Tạo notebook NumPy kickoff, thực hành 1D/2D array + shape/dtype/ndim + slicing + boolean mask + vectorized ops + aggregation + mini case | Evidence: output đúng + Restart & Run All + git log | Notes/Issues: phân biệt list vs np.array, kiểm tra shape/dtype khi debug | Next: NumPy thực hành (broadcasting, aggregation theo trục, 2D mask)