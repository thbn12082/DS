FT-W1D4 — 2026-01-20 — Pandas cleaning cơ bản (missing values, dtype, string cleaning, parse số)

Done: Tạo notebook cleaning basics, inspect dữ liệu bẩn, clean text, parse amount sang numeric, xử lý missing bằng fillna/dropna, mini case filter/summarize sau cleaning, verify Run All + notes + commit | Evidence: amount dtype numeric, dropna amount còn 5 dòng, mini case count=1 total=200 mean=200, git log | Notes/Issues: luôn inspect trước khi clean; parse numeric bằng to_numeric(coerce); dùng dropna(subset) thay vì dropna toàn bảng | Next: Pandas EDA core (groupby, value_counts, sort_values, top-N)


